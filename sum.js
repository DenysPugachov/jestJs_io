const data = { one: 1 }
const n = null
const z = 0
const floatingValue = 0.1 + 0.2
const shoppingList = [
  'diapers',
  'kleenex',
  'trash bags',
  'paper towels',
  'beer',
];



data["two"] = 2

function compileAndroidCode() {
  throw new Error('you are using the wrong JDK');
}

function sum(a, b) {
  return a + b
}

module.exports = { sum, data, n, z, floatingValue, shoppingList, compileAndroidCode }